#ifndef  Algos
#define Algos

#define _CRT_SECURE_NO_WARNINGS

#include <iostream>
#include <vector>
#include <cstdio>
#include <algorithm>
#include <cmath>

using namespace std;


void smooth_sort(vector<int>& mas);
void merge_sort(vector<int>& arr, int low, int high);
void bucketSort(vector<int>& arr, int n, int);
#endif // ! 
